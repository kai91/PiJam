using UnityEngine;
using System.Collections;

public class TapDetector : MonoBehaviour {

    float minimum = 1;
    float maximum = 5;

	// Use this for initialization
	void Start () {
       
	
	}
    
    void Update()
    {
        //var originalScale = transform.localScale;
        //var targetScale = originalScale + new Vector3(1.0f, 0.0f, 1.0f);
        //transform.localScale = Vector3.Lerp(originalScale,targetScale,0.01f);
    }
}
